{
  "name": "upcoming-matches",
  "version": "1.0.0",
  "description": "Football matches application using React and Express",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "vercel-build": "cd client && npm ci && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "engines": {
    "node": ">=14.x"
  },
  "keywords": ["football", "matches", "api", "react", "tailwind"],
  "author": "",
  "license": "MIT"
} 